2020年9月2日 面试题
1-----为什么需要数据库？都有哪些主流的数据库？它们的特点分别是什么？分别对应的语言是什么？

因为数据需要存放/查找等：

Oracle：甲骨文公司开发，收费，功能强大，语法丰富，适用于各种大中小微机环境
MySql：社区开源免费，后被甲骨文收购，但仍保留免费社区版。特点是可选择“引擎”
SQL Server：微软公司开发，收费，可视化操作界面（Management Studio）强大，之前只能在windows上部署，现在已可以在Linux上部署使用。 

 Oracle推出的被称为PL/SQL
mysql还是叫mysql，
SQL Server推出的，就叫做：T-SQL

2-----Visual Studio的LocalDB 的特点是什么？


    轻量级：不需要复杂的安装/配置/启动等
    专为开发者（developer）设计，让开发者专注于开发

3-----	                       参照                          是否会脱离文档流
固定定位 position:fixed     显示窗体屏幕                          会 

绝对定位  position:absolute 最近一个定位的父元素，直到最外层元素    会
	
	


相对定位  position:relative  本来应该在的位置                     不会

4-----为什么需要数据类型？常见数据类型都有哪些？
答：因为有数据识别的需要，运算的需要，校验的需要，还有可以更高效的存储数据。
答：常见的数据类型有：文本类型，数值类型，日期类型，真假类型，NULL值。

5----数据的增删改查---------
NULL是什么意思?

NULL这个值表示UNKNOWN(未知):它不表示“”(空字符串)。假设您的SQL Server数据库里有ANSI_NULLS，当然在默认情况下会有，对NULL这个值的任何比较都会生产一个NULL值。
您不能把任何值与一个 UNKNOWN值进行比较，并在逻辑上希望获得一个答案。您必须使用IS NULL操作符这个值表示UNKNOWN(未知):它不表示“”(空字符串)。假设您的SQL Server数据库里有ANSI_NULLS，当然在默认情况下会有，对NULL这个值的任何比较都会生产一个NULL值。您不能把任何值与一个 UNKNOWN值进行比较，并在逻辑上希望获得一个答案。您必须使用IS NULL操作符。

6---什么是索引?SQL Server 2000里有什么类型的索引?----------------

简单地说，索引是一个数据结构，用来快速访问数据库表格或者视图里的数据。在SQL Server里，它们有两种形式:聚集索引和非聚集索引。
聚集索引在索引的叶级保存数据。这意味着不论聚集索引里有表格的哪个(或哪些)字段，这些字段都会按顺序被保存在表格。由于存在这种排序，所以每个表格只会有一个聚集索引。
非聚集索引在索引的叶级有一个行标识符。这个行标识符是一个指向磁盘上数据的指针。它允许每个表格有多个非聚集索引。

7---什么是事务？有什么作用？-----------------
格式如下：
 1---begin tran
 2----要执行的代码
 3---rollback ///可以回滚回原先状态
 4---commit   ////如果确认数据无误，可以commit
-事务就是被绑定在一起作为一个逻辑工作单元的SQL语句分组，如果任何一个语句操作失败那么整个操作就被失败，以后操作就会回滚到操作前状态，或者是上有个节点。
为了确保要么执行，要么不执行，就可以使用事务

8------GO语句是一条sql语句吗？它有什么用呢？--------------
使用 GO 来分隔SQL语句。GO并不是一条SQL语句，而是一个对SQL Server的客户端（比如VS的new query窗口）有效的命令。
客户端检测到 GO，就会以GO为分隔符，将SQL语句分隔成若干批发送给SQL Server，由SQL Server分批处理

